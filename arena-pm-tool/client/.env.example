# ===========================================
# ARENA PM TOOL CLIENT - DEVELOPMENT
# ===========================================
# Copy this file to .env and update the values

# Backend API URL
# For local development, set this to your local server:
REACT_APP_API_URL=http://localhost:5001/api
# For Vercel deployment: Do NOT set this (or leave empty) - the app will use
# relative /api paths which work when frontend and backend share the same domain.

# Supabase Configuration (optional - for Supabase integration)
# Get these values from your Supabase project: Settings > API
# NOTE: Only include the anon key here - NEVER the service role key
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_anon_key_here

# ===========================================
# VERCEL DEPLOYMENT NOTES (Client)
# ===========================================
# Environment variables to set in Vercel Dashboard:
#
# OPTIONAL (only if using Supabase client-side):
# - REACT_APP_SUPABASE_URL
# - REACT_APP_SUPABASE_ANON_KEY
#
# DO NOT set REACT_APP_API_URL in Vercel - relative paths work automatically
