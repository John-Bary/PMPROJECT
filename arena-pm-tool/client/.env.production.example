# ===========================================
# TODORIA CLIENT - PRODUCTION
# ===========================================
# For local production builds: copy to .env.production.local
# For Vercel: set these in the Vercel dashboard under Project Settings > Environment Variables

# ===========================================
# MONOREPO DEPLOYMENT (Recommended)
# ===========================================
# When deploying as a monorepo (frontend + backend in one Vercel project):
# DO NOT set REACT_APP_API_URL - it defaults to "/api" which works with same-domain routing
#
# The vercel.json routes handle:
#   /api/* -> serverless backend
#   /*     -> frontend SPA

# ===========================================
# SEPARATE DEPLOYMENTS (Alternative)
# ===========================================
# Only set this if deploying frontend and backend as SEPARATE Vercel projects:
# REACT_APP_API_URL=https://your-server-project.vercel.app/api

# ===========================================
# SUPABASE CLIENT (Optional)
# ===========================================
# Only needed if using Supabase directly from the browser (realtime, storage, etc.)
# These are safe to expose - they're protected by Row Level Security (RLS)
# REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
# REACT_APP_SUPABASE_ANON_KEY=your_anon_key_here

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. REACT_APP_* variables are embedded at BUILD TIME, not runtime
# 2. Any changes require a redeploy to take effect
# 3. Never put secrets in REACT_APP_* variables - they're visible in browser
